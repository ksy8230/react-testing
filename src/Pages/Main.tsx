import React from 'react';
import TodoTable from '../Components/TodoTable';

const Main = (): JSX.Element => {
  return (
    <div>
      <TodoTable data={[]} />
    </div>
  );
};

export default Main;
